<h1>New Employee role</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.employee_role_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.employee_role_path(@conn, :index) %></span>
