<h1>Edit Job category</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.job_category_path(@conn, :update, @job_category)) %>

<span><%= link "Back", to: Routes.job_category_path(@conn, :index) %></span>
